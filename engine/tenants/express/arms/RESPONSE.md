# RESPONSE — the response (`express.response`)

> Load before touching this module. Hand-cut by module because the walk rules express one pillar;
> the generated region at the bottom is the walk's own inventory and the re-walk.

## ⚖ The law of this arm
`res.send` · `json` · `jsonp` · `sendFile` · `download` · `render` · `redirect` · `cookie` / `clearCookie` · `format` · `attachment` · `append` · `set`/`get` · `links` · `location` · `vary` · `type` · `contentType` · `status` · `sendStatus`. The ring supplies `send`, `content-disposition`, `cookie`, `encodeurl`, `escape-html`, `etag`, `mime-types`, `on-finished`, `statuses`, `vary`.

<!-- graphy:arm RESPONSE generated — do not edit inside this region; `graphy arms` regenerates it, `graphy arms --verify` names drift
     store=ecab7ec77bc6bdf7 cut=sha256:c839152b4faa5af6a6cfb0fe78d6fe50195eae3b0092a5e29778f9a673abf337 content=sha256:9c8831af7c36ee9b27e6693e1e9d42fb7d418fcb348c960f94c52201014921fd -->
## The walk's inventory — generated

Every symbol the partition places in this arm, by module; a class carries its method count. The prose above is judgment; this region is the walk, re-rendered from the store on every rebuild and refused when it drifts.

**`express.response`** — functions: `append` · `attachment` · `clearCookie` · `contentType` · `cookie` · `download` · `format` · `get` · `json` · `jsonp` · `links` · `location` · `redirect` · `render` · `send` · `sendFile` · `sendStatus` · `set` · `status` · `vary` · `sendfile` · `stringify`

## The inherits joins out — generated

(no inherits edge leaves this arm)

## Re-walk — generated

```bash
T=tenants/express
python3 -m graphy pillars --tenant $T/tenant.json --tenant-id express --corpus express --against $T/partition.json
python3 -m graphy arms --tenant $T/tenant.json --tenant-id express --corpus express --partition $T/partition.json --dir $T/arms --verify
```

<!-- /graphy:arm RESPONSE -->
