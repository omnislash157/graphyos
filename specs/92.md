# hunt is a CLI verb, and no MCP tool drifts from its terminal twin
issue: graphyos #92
host: linux, windows
scars: S1 S8 S5 S4 S6
recurs: #89 r0 B1, #92 r0 B1

## Contract
- C1 `graphy hunt <name> --tenant … --tenant-id …` prints the bytes the MCP `hunt` tool returns: by tail, by substring, and the named empty with exit 1
- C2 every tool in `mcp.TOOLS` is a CLI verb and every argument it takes is a dest that verb reads; `review.py mcp-cli-twins` refuses a drift
- C3 README and CLAUDE.md name `hunt` as a verb, and every advertised `graphy hunt` line parses

## Scope
- IN: engine/graphy/doors.py
- IN: engine/graphy/cli.py
- IN: engine/graphy/mcp.py
- IN: engine/tests/test_mcp.py
- IN: review.py
- IN: README.md
- IN: CLAUDE.md

## Hazards <!-- spec_lint: waive R7 — W1 W3 W4 W5 E2 E3 G1: this rung writes no bytes, runs no hook, lands no generation and reads no path; it adds a read-only verb over the store the doors already open -->
- W6 C1 P1
- W7 C1 P4
- E1 C1 P1
- F1 C2 P2
- C1 C2 P2
- D1 C1 P3

## Probes
```bash
# P1 C1 W6 E1
cd engine && ../.venv/bin/python -m pytest -q -o addopts="" tests/test_mcp.py -k "hunt_at_the_terminal_prints_the_mcp_tools_answer"
G="$PWD/.venv/bin" && T=$(mktemp -d) && git clone -q https://github.com/pallets/itsdangerous "$T/repo" && cd "$T/repo" && "$G"/graphy eat . --no-provision
for n in 1 2 3; do "$G"/graphy hunt Signer --tenant "$PWD/.graphy/tenant.json" --tenant-id itsdangerous | grep -q "itsdangerous://class/itsdangerous.signer.Signer"; done
# P2 C2 F1 C1
python3 review.py --selftest 2>&1 | grep -q "ok  mcp-cli-twins"
python3 review.py 2>&1 | grep -q "REVIEW OK"
# P3 C1 C3 D1
.venv/bin/graphy hunt zzz_nothing --tenant engine/tenants/graphy/tenant.json --tenant-id graphy; test $? -eq 1
python3 review.py 2>&1 | grep "advertised-argv-parses" | grep -qv FAIL
# P4 C1 W7
gh workflow run ci.yml --repo omnislash157/graphy --ref main
gh run watch "$(gh run list --repo omnislash157/graphy --branch main --limit 1 --json databaseId --jq '.[0].databaseId')" --repo omnislash157/graphy --exit-status
```

## Production
```bash
# host: linux a fresh clone of a real repo, eaten, and the first door the orientation names typed at a terminal
G="$PWD/.venv/bin" && rm -rf /tmp/p92 && git clone -q https://github.com/pallets/itsdangerous /tmp/p92/repo && cd /tmp/p92/repo
"$G"/graphy eat . --no-provision && "$G"/graphy hunt Signer --tenant "$PWD/.graphy/tenant.json" --tenant-id itsdangerous
# host: windows the production Windows seat, a fresh clone, eaten, hunt typed at the console
graphy eat . --no-provision && graphy hunt Signer --tenant "$PWD\.graphy\tenant.json" --tenant-id itsdangerous
```
